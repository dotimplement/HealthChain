# Healthcare Data Converter Dependencies
# Install healthchain from parent directory or PyPI

# Core dependencies
fastapi>=0.115.3,<0.116
uvicorn>=0.24.0,<0.25
pydantic>=2.0.0,<2.11.0
pyyaml>=6.0.3,<7

# XML processing
lxml>=5.2.2,<6
xmltodict>=0.13.0,<0.14

# FHIR support
fhir-resources>=8.0.0,<9

# Template engine
python-liquid>=1.13.0,<2

# HTTP client
httpx>=0.27.0,<0.28

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0

# HealthChain (install from parent directory or PyPI)
# pip install ../  OR  pip install healthchain
