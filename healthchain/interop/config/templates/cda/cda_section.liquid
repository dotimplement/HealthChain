{
  "section": {
    "templateId": {
      "@root": "{{ config.template_id }}"
    },
    "code": {
      "@code": "{{ config.code }}",
      "@codeSystem": "2.16.840.1.113883.6.1",
      "@codeSystemName": "LOINC",
      "@displayName": "{{ config.display }}"
    },
    "title": "{{ config.display }}",
    "entry": {{ entries | json }}
  }
}
